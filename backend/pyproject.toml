[project]
name = "jockey-demo-backend"
version = "0.1.0"
description = "Sponsor Spotlight demo — Jockey-only backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
    "pydantic>=2.7",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
