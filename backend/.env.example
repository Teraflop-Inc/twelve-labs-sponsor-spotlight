# Sponsor Spotlight — local configuration
#
#   cd backend && cp .env.example .env
#
# Loaded by core/env.py from backend/.env. This file is OPTIONAL — exported
# environment variables always win over it, so `export TWELVELABS_API_KEY=...`
# (or sourcing a shell script that does) works just as well and no .env is
# needed. On Vercel there is no .env; set these as project env vars instead.


# ── Required ──────────────────────────────────────────────────────────────────

# Your TwelveLabs API key. Jockey (jockey1.0) is in beta and needs an
# allowlisted key — https://twelvelabs.io
#
# Used by the offline scripts (pre_processing/) and by the running app for every
# Jockey call. There is no in-browser key entry — without this the app cannot
# analyze anything. On a public deployment every visitor's request spends it.
TWELVELABS_API_KEY=


# Lock the app to one collection.
#
#   True  (default)  Sales demo. Pinned to SPONSOR_SPOTLIGHT_STORE_ID below with
#                    a fixed game roster; the collection picker is hidden and the
#                    collection-management endpoints return 403. Results come
#                    from the committed fixtures, so the flow renders instantly.
#
#   False            Working app. Every knowledge store in the account is listed
#                    and selectable, you can create collections and attach
#                    assets, and each game in the loaded store can be analyzed.
#                    Results are computed live (~45s-3min per call).
#
# Both modes use TWELVELABS_API_KEY above; this only controls what is allowed.
# DEMO_MODE=True


# ── Demo collection (optional) ────────────────────────────────────────────────
# Defaults point at our pre-indexed Premier League collection, which your key
# cannot read. Set these to your own knowledge store to run the demo tab on your
# own footage — but see the warning below first.

# Knowledge store the Demo tab is pinned to.
# SPONSOR_SPOTLIGHT_STORE_ID=ks_...

# Display name for that collection in the UI.
# SPONSOR_SPOTLIGHT_STORE_NAME=My Broadcasts

# Sport profile driving prompts + enrichment vocabulary (see backend/sports.py).
# SPONSOR_SPOTLIGHT_SPORT=soccer

# Brands the Demo tab pre-selects and auto-runs. Must exist in the committed
# demo_fixtures/, or the request falls through to a live Jockey call.
# SPONSOR_SPOTLIGHT_DEMO_BRANDS=Etihad,Emirates

# Per-game roster. Replaces the bundled Premier League list entirely (no
# merging). A file at backend/games.json of the same shape works too; this env
# var wins over it. Each entry needs id + asset_id; label defaults to id.
# SPONSOR_SPOTLIGHT_GAMES=[{"id":"wk1","asset_id":"6a2...","label":"Week 1"}]

# Note: the committed fixtures in backend/demo_fixtures/ record the knowledge
# store they were captured from. Point SPONSOR_SPOTLIGHT_STORE_ID at your own
# store and they stop matching, so every request runs live against your footage
# instead of replaying ours. That is the intended behaviour — expect Jockey
# latency (~45s-3min per call) rather than the instant cached demo.


# ── Highlight reels (optional) ────────────────────────────────────────────────
# Only needed to run pre_processing/build_reels.py. Requires FFmpeg on PATH.

# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...


# ── Offline capture knobs (optional) ──────────────────────────────────────────
# See pre_processing/capture_demo_cache.py and build_reels.py for the full set.

# SPONSOR_SPOTLIGHT_CAPTURE_GAMES=aggregate,mci-liv-2019
# SPONSOR_SPOTLIGHT_CAPTURE_BRANDS=Etihad,Emirates
# SPONSOR_SPOTLIGHT_CAPTURE_DELAY=30      # seconds between calls (~2 req/min)
# SPONSOR_SPOTLIGHT_ANALYZE_TOPN=8
# SPONSOR_SPOTLIGHT_LEGIBILITY_TOPN=8
# SPONSOR_SPOTLIGHT_CAPTURE_FORCE=1       # re-capture everything from scratch
